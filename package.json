{
  "name": "react-wash-icons",
  "version": "1.0.0",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/react-wash-icons.cjs",
  "module": "./dist/react-wash-icons.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/react-wash-icons.js"
      },
      "require": {
        "types": "./dist/index.d.ts",
        "default": "./dist/react-wash-icons.cjs"
      }
    }
  },
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/react": "^19.2.14",
    "fast-xml-parser": "^5.5.9",
    "prettier": "^3.8.1",
    "react": "^19.2.4",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  },
  "scripts": {
    "generate": "tsx scripts/generate-icons.ts",
    "build": "npm run generate && vite build && tsc --project tsconfig.build.json --emitDeclarationOnly",
    "format": "prettier --write \"src/**/*.{ts,tsx}\" \"scripts/**/*.ts\"",
    "prepublishOnly": "npm run build",
    "release:patch": "npm version patch && git push origin master --tags",
    "release:minor": "npm version minor && git push origin master --tags",
    "release:major": "npm version major && git push origin master --tags"
  }
}
